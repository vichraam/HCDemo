import { PeopleFilterPipe } from './people-filter.pipe';

describe('PeopleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PeopleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
